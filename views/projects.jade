ul#projects-list
  each project in projects
    li(class='project')
      .inner
        .left
        .right
        //label= project.name
        //label= project.description
        img(src= project.image_url)